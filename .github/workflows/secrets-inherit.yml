# VULNERABLE: Secrets inheritance to external workflow
name: Call External Workflow

on:
  push:
    branches: [main]

jobs:
  call-external:
    uses: some-other-org/workflows/.github/workflows/build.yml@main
    secrets: inherit  # VULNERABLE: All secrets passed to external workflow
